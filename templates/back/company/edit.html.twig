{% extends 'admin.html.twig' %}

{% block title %}PlumbPay - Admin - Modifier Entreprise{% endblock %}

{% block body %}
    {{ include('/back/company/_nav.html.twig') }}
    
    <!-- Header Section -->
    <div class="mb-6">
        <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Modifier l'entreprise</h1>
        <p class="text-gray-600 dark:text-gray-400 mt-1">Modifiez les informations de l'entreprise {{ company.name }}.</p>
    </div>

    <!-- Form Container -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6">
        {{ include('back/company/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
{% endblock %}
