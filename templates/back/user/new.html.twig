{% extends 'admin.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <h1>Create new User</h1>
    <div class="flex flex-row gap-4">
        <a href="{{ path('back_app_user') }}" class="btn btn-all">All users</a>
    </div>
    {{ include('back/user/_form.html.twig') }}

{% endblock %}
