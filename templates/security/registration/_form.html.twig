{{ form_start(registrationForm) }}
    <div class="flex flex-col gap-4">
        {{ form_widget(registrationForm) }}
        <div class="grid grid-cols-2 gap-4">
            <button type="submit" class="px-4 py-2 text-center rounded-lg bg-mainColor-400 text-TextColor-800 hover:bg-secondaryColor-400 focus:bg-secondaryColor-400">Register</button>
            <a href="{{ path('app_login') }}" class="px-4 py-2 text-center rounded-lg bg-secondaryColor-400 text-TextColor-800 hover:bg-mainColor-400 focus:bg-mainColor-400">Sign In</a>
        </div>
    </div>
{{ form_end(registrationForm) }}