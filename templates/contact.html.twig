<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome to Plumbpay{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <main class="h-screen flex items-center relative">
            <div class="absolute top-0 left-0 w-full flex flex-col lg:flex-row h-full">
                <div class="w-full lg:w-1/3 bg-mainColor-400 h-full"></div>
                <div class="bg-bgBody-400 dark:bg-bgBody-800 h-full"></div>
            </div>
            <div class="absolute w-full mx-auto">
                <div class="grid lg:grid-cols-2 gap-12 lg:max-w-6xl mx-auto w-full px-4 lg:px-0">
                    <div class="w-full h-full">
                        <a href="{{ path('front_default_index') }}">
                            <img src="{{ asset('img/logo-plumbpay.png') }}" alt="logo plumbpay" class="w-full h-full">
                        </a>
                    </div>
                    <div>
                        {% block body %}{% endblock %}
                    </div>
                </div>
            </div>
        </main>
    </body>
</html>
