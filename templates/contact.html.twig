<!DOCTYPE html>
<html lang="{{ app.request.locale }}" class="h-full">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Bienvenue sur Plumbpay{% endblock %}</title>
        <link rel="icon" href="/plombier.png" type="image/png">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        {% block javascripts %}
            <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="bg-bgBody-400 dark:bg-bgBody-800 text-TextColor-400 dark:text-TextColor-800 font-sans transition-colors duration-300 overflow-y-auto">
        <main class="min-h-screen flex items-start justify-center p-4 sm:p-6 lg:p-8 py-8">
            <div class="w-full max-w-7xl mx-auto">
                <div class="flex flex-col gap-8 lg:gap-12 lg:flex-row items-start lg:items-center">
                    <div class="hidden lg:flex flex-col justify-center gap-8 text-gray-900 lg:max-w-lg dark:text-gray-100">
                        <div class="">
                            <a href="{{ path('front_default_index') }}" class="inline-flex items-center group">
                                <div class="relative mr-4">
                                    <img src="{{ asset('img/logo-plumbpay-transparent.png') }}" alt="PlumbPay Logo" class="w-16 h-16 transition-transform duration-300 group-hover:scale-110 dark:hidden">
                                    <img src="{{ asset('img/logo-plumbpay.png') }}" alt="PlumbPay Logo" class="w-16 h-16 transition-transform duration-300 group-hover:scale-110 hidden dark:block">
                                    <div class="absolute inset-0 bg-mainColor-400/20 dark:bg-mainColor-400/15 rounded-full blur-md opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                                </div>
                                <div>
                                    <h1 class="text-4xl font-bold tracking-tight">PlumbPay</h1>
                                    <p class="text-gray-600 dark:text-gray-300 text-lg">Facturation Professionnelle</p>
                                </div>
                            </a>
                        </div>
                        <div class="flex flex-col gap-6">
                            <div class="flex flex-col gap-4">
                                <h2 class="text-3xl font-bold leading-tight">
                                    Nous Sommes 
                                    <span class="bg-gradient-to-r from-mainColor-400 to-secondaryColor-400 bg-clip-text text-transparent">
                                        Là Pour Vous
                                    </span>
                                </h2>
                                <p class="text-xl text-gray-700 dark:text-gray-200 leading-relaxed">
                                    Notre équipe d'experts est disponible pour répondre à toutes vos questions et vous accompagner dans votre réussite.
                                </p>
                            </div>
                            <div class="flex flex-col gap-3">
                                <div class="flex items-center gap-3 text-gray-700 dark:text-gray-200">
                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'check-circle', class: 'w-5 h-5 text-green-500 dark:text-green-400' }) }}
                                    <span>Support technique personnalisé</span>
                                </div>
                                <div class="flex items-center gap-3 text-gray-700 dark:text-gray-200">
                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'check-circle', class: 'w-5 h-5 text-green-500 dark:text-green-400' }) }}
                                    <span>Réponse sous 24h garantie</span>
                                </div>
                                <div class="flex items-center gap-3 text-gray-700 dark:text-gray-200">
                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'check-circle', class: 'w-5 h-5 text-green-500 dark:text-green-400' }) }}
                                    <span>Équipe francophone dédiée</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="w-full mx-auto lg:mx-0">
                        <div class="bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-2xl shadow-lg border border-white/20 dark:border-gray-700/50 p-8 lg:p-10">
                            <div class="lg:hidden text-center mb-8">
                                <a href="{{ path('front_default_index') }}" class="inline-flex items-center justify-center group">
                                    <img src="{{ asset('img/logo-plumbpay-transparent.png') }}" alt="PlumbPay Logo" class="w-12 h-12 mr-3 dark:hidden">
                                    <img src="{{ asset('img/logo-plumbpay.png') }}" alt="PlumbPay Logo" class="w-12 h-12 mr-3 hidden dark:block">
                                    <div class="text-gray-900 dark:text-gray-100">
                                        <h1 class="text-2xl font-bold">PlumbPay</h1>
                                        <p class="text-sm text-gray-600 dark:text-gray-300">Facturation Professionnelle</p>
                                    </div>
                                </a>
                            </div>
                            <div class="flex flex-col gap-6">
                                <!-- Flash Messages -->
                                {% for type, flashMessages in app.flashes %}
                                    {% for message in flashMessages %}
                                        <div class="p-4 rounded-xl backdrop-blur-sm border-l-4 mb-4
                                            {% if type == 'success' %}bg-green-50/90 dark:bg-green-900/20 border-l-green-500 text-green-800 dark:text-green-400
                                            {% elseif type == 'error' %}bg-red-50/90 dark:bg-red-900/20 border-l-red-500 text-red-800 dark:text-red-400
                                            {% elseif type == 'warning' %}bg-yellow-50/90 dark:bg-yellow-900/20 border-l-yellow-500 text-yellow-800 dark:text-yellow-400
                                            {% else %}bg-blue-50/90 dark:bg-blue-900/20 border-l-blue-500 text-blue-800 dark:text-blue-400{% endif %}">
                                            <div class="flex items-center">
                                                {% if type == 'success' %}
                                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'check-circle', class: 'w-5 h-5 mr-3 flex-shrink-0' }) }}
                                                {% elseif type == 'error' %}
                                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'x-circle', class: 'w-5 h-5 mr-3 flex-shrink-0' }) }}
                                                {% elseif type == 'warning' %}
                                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'alert-triangle', class: 'w-5 h-5 mr-3 flex-shrink-0' }) }}
                                                {% else %}
                                                    {{ include('_composants/_lucide_icon.html.twig', { name: 'info', class: 'w-5 h-5 mr-3 flex-shrink-0' }) }}
                                                {% endif %}
                                                <div>
                                                    <p class="font-medium">{{ message }}</p>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                {% endfor %}
                                
                                {% block body %}{% endblock %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
        <div class="fixed top-4 left-4 z-10">
            <a href="{{ path('front_default_index') }}" class="group flex items-center px-4 py-3 bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm text-gray-900 dark:text-gray-200 rounded-2xl border border-white/20 dark:border-gray-700/50 hover:bg-white/90 dark:hover:bg-gray-800/90 transition-all duration-300 shadow-lg hover:shadow-xl">
                {{ include('_composants/_lucide_icon.html.twig', { name: 'arrow-left', class: 'w-4 h-4 mr-2 transition-transform duration-300 group-hover:-translate-x-1' }) }}
                <span class="font-medium">Retour à l'accueil</span>
            </a>
        </div>
        <div class="fixed top-4 right-4 z-10">
            {{ include('_composants/_theme_toggle.html.twig') }}
        </div>
    </body>
</html>
