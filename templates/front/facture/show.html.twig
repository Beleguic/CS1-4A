{% extends 'base.html.twig' %}

{% block title %}Facture {{ facture.id }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Facture {{ facture.id }}</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ facture.id }}</td>
                </tr>
                <tr>
                    <th>Client Name</th>
                    <td>{{ facture.devis.clientName }}  {{ facture.devis.Client }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ facture.date ? facture.date|date('Y-m-d H:i:s') : 'N/A' }}</td>
                </tr>
                <tr>
                    <th>Amount</th>
                    <td>{{ facture.amount is not null ? facture.amount : 'N/A' }}</td>
                </tr>
                <tr>
                    <th>Taxes</th>
                    <td>{{ facture.devis.taxes }}</td>
                </tr>
                <tr>
                    <th>Paid</th>
                    <td>{{ facture.paid ? 'Yes' : 'No' }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('front_app_facture_index') }}" class="btn btn-secondary">Back to List</a>

        <a href="{{ path('front_app_facture_edit', {'id': facture.id}) }}" class="btn btn-primary">Edit</a>

        {{ include('/front/facture/_delete_form.html.twig') }}
    </div>
{% endblock %}