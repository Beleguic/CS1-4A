{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_app_devis_index') }}">back to list</a>

    <a href="{{ path('front_app_devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('/front/devis/_delete_form.html.twig') }}
{% endblock %}
