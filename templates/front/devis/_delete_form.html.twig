<form method="post" action="{{ path('front_app_devis_delete', {'id': devis.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devis.id) }}">
    <button class="btn">Delete</button>
</form>
